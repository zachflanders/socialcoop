import React from 'react';
import Typography from '@material-ui/core/Typography';

const Home = () => (
  <div style={{paddingTop: '32px'}}>
  <Typography variant="h4" component="h1" style={{textAlign:'center', marginBottom:'32px'}}>
    Home
  </Typography>

  </div>
)

export default Home;
